(function(){const b=document.querySelector('.icon-btn');if(!b)return;b.title='API 配置';b.onclick=()=>{let m=document.querySelector('.config-modal');if(m){m.remove();return}m=document.createElement('div');m.className='config-modal';m.innerHTML='<div class="config-dialog"><div class="config-dialog-head"><div><span class="section-kicker">SETTINGS / API</span><h2>模型与 API 配置</h2></div><button class="close-config">×</button></div><p class="config-help">第一版只配置自己的中转站 Key，不需要登录。</p><label>图像模型<input class="model-input" value="gpt-image-2-1k" /></label><label>API Endpoint<input class="endpoint-input" value="https://image.cjyyswq.com/v1" /></label><label>API Key<input class="key-input" type="password" placeholder="输入中转站 API Key" /></label><div class="quota-row"><span>额度状态<br><b>尚未查询</b></span><button class="ghost-btn check-quota">测试连接 / 查询额度</button></div><p class="key-note">Key 仅保存在本机，不会写入项目文件。</p><div class="config-actions"><button class="ghost-btn close-config">取消</button><button class="primary-btn save-config">保存配置</button></div></div>';document.body.append(m);m.querySelectorAll('.close-config').forEach(x=>x.onclick=()=>m.remove());m.querySelector('.save-config').onclick=()=>{localStorage.setItem('atelier-api',JSON.stringify({model:m.querySelector('.model-input').value,endpoint:m.querySelector('.endpoint-input').value,key:m.querySelector('.key-input').value}));m.remove();toast('配置已保存')};m.querySelector('.check-quota').onclick=()=>toast('额度查询将在接入中转站余额接口后启用')}})();
